const Footer = () => {
  return (
    <div style={{ marginTop: "20px", fontSize: "14px" }}>
      Try typing something in the editor above and observe the logged
      EditorState in your browser console.
    </div>
  );
};

export default Footer;
